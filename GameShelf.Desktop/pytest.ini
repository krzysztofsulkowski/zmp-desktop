[pytest]
testpaths = tests
python_files = test_*.py
markers =
    integration: optional tests that require a running local API
    e2e: optional end-to-end tests that require a running API and a test user
